---
title: Hr
description: Creates divider (horizontal rule) that separates content
type: component
---

import Install from '@components/mdx/install.mdx';

<h1>{frontmatter.title}</h1>
<p>{frontmatter.description}</p>

<Install type={frontmatter.type} />

## Usage

```jsx "Hr"
// HrExample.jsx
import { Hr } from 'jsx-email';

const Email = () => {
  return (
    <>
      <p>Content above</p>
      <Hr />
      <p>Content below</p>
    </>
  );
};
```

## Props

### Optional Props

#### disableDefaultStyle

```ts
disableDefaultStyle?: boolean;
```

When `true`, removes default styles. Useful when using [`Tailwind`](/components/tailwind) or custom classes.

### Additional Props

This component expresses all of the [Common Component Props](https://react.dev/reference/react-dom/components/common) for `ComponentProps<'hr'>`.
