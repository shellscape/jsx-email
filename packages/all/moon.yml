# https://moonrepo.dev/docs/config/tasks
$schema: 'https://moonrepo.dev/schemas/tasks.json'

workspace:
  inheritedTasks:
    exclude: ['test']

tasks:
  bump:
    command: date +%s > .bumpfile
    platform: system
    options:
      cache: false
